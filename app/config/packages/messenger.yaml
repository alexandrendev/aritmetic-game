framework:
    messenger:
        transports:
            async: '%env(MESSENGER_TRANSPORT_DSN)%'
            sync: 'sync://'

        routing:
            App\Message\CloseGameRoundMessage: async

when@test:
    framework:
        messenger:
            transports:
                async: 'in-memory://'
