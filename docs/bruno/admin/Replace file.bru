meta {
  name: Replace file
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/admin/files/{{fileId}}
  body: multipart-form
  auth: inherit
}

body:multipart-form {
  file: @file()
}

settings {
  encodeUrl: true
  timeout: 0
}
