meta {
  name: refresh
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/token/refresh
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "refresh_token": "{{refreshToken}}"
  }
}
